look at main.c and entity/costs.c and data/heap.c
uses a* with distance heuristic that takes into account the difference between the height and width of characters compared to squares
switch heuristic in costs.c to zeroDistance to use dijkstra's instead of a*
heap is pairing heap
