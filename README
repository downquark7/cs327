defeated npcs will move until not on a road
f will request 2 numbers seperated by a space to fly to map
doesn't really use inheritance I spent so much time getting function pointers to work I want to use them for now

--numtrainers will never crash when a reasonable amount of memory is available
added test argument as --test number
will just run number times of number iterations with random moves to check for random errors
--waitForDebug will wait for a couple character presses so a debugger can be attached